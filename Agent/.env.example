# =============================================================================
# Shader Copilot Agent - Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your API keys
# DO NOT commit .env to version control

# -----------------------------------------------------------------------------
# WebSocket Server Configuration
# -----------------------------------------------------------------------------
WEBSOCKET_HOST=localhost
WEBSOCKET_PORT=8765

# -----------------------------------------------------------------------------
# LLM API Configuration
# -----------------------------------------------------------------------------
# OpenAI-compatible API base URL (required)
# Examples:
#   - OpenAI: https://api.openai.com/v1
#   - Azure OpenAI: https://<resource>.openai.azure.com
#   - Local LLM: http://localhost:8080/v1
LLM_API_BASE=https://api.openai.com/v1

# API Key (required)
LLM_API_KEY=sk-your-api-key-here

# -----------------------------------------------------------------------------
# Model Configuration
# -----------------------------------------------------------------------------
# Router model - lightweight, for intent classification
# Default: qwen-turbo (or gpt-3.5-turbo for OpenAI)
ROUTER_MODEL=qwen-turbo

# Code model - high quality code generation
# Default: qwen-max (or gpt-4 for OpenAI)
CODE_MODEL=qwen-max

# VL (Vision-Language) model - image understanding
# Default: qwen-vl-plus (or gpt-4-vision-preview for OpenAI)
VL_MODEL=qwen-vl-plus

# -----------------------------------------------------------------------------
# Generation Settings
# -----------------------------------------------------------------------------
# Maximum retry attempts for shader compilation
MAX_RETRY_COUNT=3

# LLM request timeout in seconds
LLM_TIMEOUT=120

# Temperature for code generation (0.0-1.0, lower = more deterministic)
CODE_TEMPERATURE=0.2

# Temperature for router/classification (0.0-1.0)
ROUTER_TEMPERATURE=0.0

# -----------------------------------------------------------------------------
# Session Configuration
# -----------------------------------------------------------------------------
# Session storage directory (relative to Unity project)
# Will be created under: Assets/ShaderCopilot/Sessions/
SESSION_STORAGE_PATH=Sessions

# -----------------------------------------------------------------------------
# Logging Configuration
# -----------------------------------------------------------------------------
# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# Log file path (optional, logs to console if not set)
# LOG_FILE=shader_copilot.log
